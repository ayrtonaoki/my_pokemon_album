<h1>Site::Home#index</h1>
<p>Find me in app/views/site/home/index.html.erb</p>

<% if user_signed_in? %>
  <p>
    <%= "#{current_user.email} is ONLINE" %>
  </p>
<% end %>

<% if admin_signed_in? %>
  <p>
    <%= "#{current_admin.email} is ONLINE" %>
  </p>
<% end %>